from e7 import has_33
from e2 import forengith_2_celsius

# В файлах на которые мы ссылаемся мы уже вызывали функцию
# Поэтому сначала выдается результат от туда, а потом новый
print("------------------------")
  
print(has_33([3, 3, 1]))          # → True
forengith_2_celsius(100)          # → 37.(7)